if (FIPS_IMPORT)
    fips_begin_lib(ozzanimation)
    fips_src(ozz-animation/src/animation/runtime)
    fips_src(ozz-animation/src/base)
    fips_src(ozz-animation/src/geometry/runtime)
    fips_src(ozz-animation/src/options)
    fips_dir(ozz-animation/samples/framework)
    fips_files(mesh.cc mesh.h)
    fips_end_lib()
    target_include_directories(ozzanimation SYSTEM PRIVATE ozz-animation/src)
    target_include_directories(ozzanimation SYSTEM PUBLIC ozz-animation/samples)
    target_include_directories(ozzanimation SYSTEM PUBLIC ozz-animation/include)
    if (FIPS_GCC)
        target_compile_options(ozzanimation PRIVATE -Wno-ignored-attributes -Wno-type-limits)
    endif()
endif()